import dashboard from './dashboard';
import competition from './competition';
import golfEvent from './golfEvent';

const menuItems = {
  items: [dashboard, competition, golfEvent]
};

export default menuItems;
